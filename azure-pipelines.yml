trigger:
- main

pool:
  vmImage: 'ubuntu-latest'

steps:
- script: | 
  echo "Installing Python 3.8"
  sudo apt install python -y 
  python --version
  python helloworld.py  
  displayName: 'python' 
